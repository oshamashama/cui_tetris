# cui_tetris
tetris run on the cui



# 起動方法

```
g++ tetris.cpp -o tetris && ./tetris
```

# 操作方法

`a`を入力するとブロックが左に
`s`で下に
`d`で左に
`S`で一番下に移動します．

`r`で回転します．（回転については判定が甘く中央寄りにいないと回転できないことも多くあります．


# 終了方法

mac OS であれば `ctrl + C` で終了することができます．